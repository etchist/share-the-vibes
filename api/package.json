{
  "name": "@vibe-coding-horrors/api",
  "version": "0.0.1",
  "private": true,
  "nx": {
    "targets": {
      "serve": {
        "continuous": true,
        "executor": "@nx/js:node",
        "defaultConfiguration": "development",
        "dependsOn": [
          "build"
        ],
        "options": {
          "buildTarget": "@vibe-coding-horrors/api:build",
          "runBuildTargetDependencies": false
        },
        "configurations": {
          "development": {
            "buildTarget": "@vibe-coding-horrors/api:build:development"
          },
          "production": {
            "buildTarget": "@vibe-coding-horrors/api:build:production"
          }
        }
      }
    }
  }
}
